services:
  nginx:
    image: nginx:latest
    container_name: project11-nginx
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped
